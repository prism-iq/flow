// conditions.flow - Logic with natural words

to check age years:
    if years < 0:
        say "Invalid age"
    otherwise if years < 13:
        say "Child"
    otherwise if years < 20:
        say "Teenager"
    otherwise if years < 65:
        say "Adult"
    otherwise:
        say "Senior"

to check access logged and admin:
    if logged and admin:
        say "Full access"
    otherwise if logged and not admin:
        say "Limited access"
    otherwise:
        say "No access"

to start:
    check age 8
    check age 16
    check age 35
    check age 70

    say "---"

    check access yes and yes
    check access yes and no
    check access no and no
