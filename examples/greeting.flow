// greeting.flow - Functions and string interpolation

do greet(name):
    say "Hello, #{name}!"

do greet_age(name, age):
    say "#{name} is #{age} years old"

do main:
    greet("World")
    greet("Flow")

    greet_age("Alice", 30)
    greet_age("Bob", 25)
